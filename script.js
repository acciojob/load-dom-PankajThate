//your JS code here. If required.
   document.addEventListener("DOMContentLoaded", function() {
        // DOM is loaded, write the message
        document.body.textContent = "DOM load success";
      });